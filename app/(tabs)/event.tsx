import { Text } from "@react-navigation/elements";
import { SafeAreaView } from "react-native-safe-area-context";

export default function EventFakePage() {
  return (
    <SafeAreaView>
      <Text>Hello</Text>
    </SafeAreaView>
  );
}
